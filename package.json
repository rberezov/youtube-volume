{
  "name": "youtube-exponential-volume",
  "version": "1.34.0",
  "private": true,
  "description": "Chrome-расширение: экспоненциальная регулировка громкости в YouTube и удлинённый точный ползунок",
  "scripts": {
    "lint": "node --check background.js && node --check bridge.js && node --check main.js && node --check popup.js && node --check preload.js && node --check worklets/loudness-meter.js",
    "test": "npm run lint && npm run test:unit && npm run test:browser",
    "test:unit": "node --test tests/*.test.cjs",
    "test:browser": "node tests/browser/run.js",
    "build:main": "node tools/build-main.js",
    "package": "node tools/package.js"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  },
  "engines": {
    "node": ">=22"
  }
}
